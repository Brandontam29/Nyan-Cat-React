import { hot } from 'react-hot-loader/patch';
import React from 'react';
import App from './App';

const Root = () => (
    <React.StrictMode>
        <App />
    </React.StrictMode>
);
export default Root;
